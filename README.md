# Таск 2 условные конструкции

Задания описаны в функциях. Файл с их использованием описан в intmain.cpp
1. Внутри intmain.cpp 10 функций как и 10 заданий чисто содержит примерные значения **для их использования**
    - intmain.cpp внутри содержит
```cpp
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    // Задание 1
    checkTriangleType(3, 4, 5);

    // Задание 2
    checkTriangleAngleType(3, 4, 5);

    // Задание 3
    checkTriangleByCoordinates(0, 0, 3, 0, 0, 4);

    // Задание 4
    checkTriangleSidesEquation(5, 7, 10);

    // Задание 5
    checkQuadrilateralType(0, 0, 4, 0, 4, 3, 0, 3);

    // Задание 6
    checkBrick(3, 2, 1, 4, 2);

    // Задание 7
    checkSphere(2, 4, 5);

    // Задание 8
    checkBox(10, 10, 10, 5, 5, 5);

    // Задание 9
    findMultiples(12, 6, 4);

    // Задание 10
    checkDivisibilityBy3(123);

    return 0;
}
```
